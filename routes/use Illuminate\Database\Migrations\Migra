use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

class CreateFeeMasterlistTable extends Migration
{
    public function up(): void
    {
        Schema::create('fee_masterlist', function (Blueprint $table) {
            $table->id();
            $table->string('category'); // e.g. business_permit, stall_rental, etc.
            $table->string('subcategory'); // e.g. fire_fee, table_rental_fish, etc.
            $table->text('description')->nullable(); // Human-friendly label
            $table->string('unit')->default('per application'); // e.g. per day, per month
            $table->decimal('amount', 10, 2)->default(0.00); // Fee amount
            $table->boolean('active')->default(true); // Enable/disable fee
            $table->timestamps();
        });
    }

    public function down(): void
    {
        Schema::dropIfExists('fee_masterlist');
    }
}
